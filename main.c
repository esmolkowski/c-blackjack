#include "game.h"

/*
void delay(int ms)
{
	clock_t start = clock();
	while (clock() < start + ms);
}
*/

void main(int argc, char *argv[]) {
	// start renderer
	runLogic();
	// stop renderer
}